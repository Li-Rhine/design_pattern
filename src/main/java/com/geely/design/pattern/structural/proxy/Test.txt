代理模式

为其他对象提供一种代理，以控制对这个对象的访问

保护目标对象
增强目标对象

静态代理：代码中显式的包装
动态代理：无法代理类，只能代理JDK生成的接口
CGLib:可以代理类通过集成、重写（注意是否是final修饰符），生成的类是代理的子类


Spring代理选择
    当Bean有实现接口时，Spring就会会JDK的动态代理
    当Bean没有实现接口时，Spring使用CGLib

JDK动态代理速度比CGLib快20%